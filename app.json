{
  "name": "simplecote-front",
  "description": "Painel e tela do representante do SimpleCote (React + Vite, SPA estatico). Backend em app Heroku separado.",
  "buildpacks": [
    { "url": "heroku/nodejs" },
    { "url": "heroku-community/static" }
  ],
  "env": {
    "VITE_API_BASE_URL": {
      "description": "URL do backend no Heroku (ex.: https://simplecote-back.herokuapp.com). Lida no `vite build`.",
      "required": true
    }
  }
}
