## 1. Coluna Quantidade no resultado

- [x] 1.1 Em `ResultadoPage.tsx`, adicionar coluna "Quantidade" (`item.quantidade`) à tabela de itens expandidos (cabeçalho + célula) — verificar que aparece entre Produto e Preço embalagem
- [x] 1.2 Teste: ao expandir um pedido, a quantidade de cada item é exibida (cobertura em `ResultadoPage.test.tsx` ou teste novo)

## 2. Checagem de saúde

- [x] 2.1 `npm test` verde (Vitest + RTL)
- [x] 2.2 `npm run build` e `npm run lint` sem erro novo
