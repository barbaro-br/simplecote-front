## 1. Módulo de leitura `src/admin/analise/`

- [x] 1.1 `analise.schema.ts`: zod para as 3 respostas — `dashboardSchema` (porStatus, contadores, `proximosPrazos[]`, gastos como `z.string()`, `topProdutos/topEmpresas` até 5), `insightProdutoSchema` + `insightProdutosMapSchema = z.record(z.string().uuid(), insightProdutoSchema)` (campos `*|null` com `.nullable()`, `serie` array 0..6), `insightEmpresaSchema` (`taxaResposta`/valores/datas nullable, `tempoMedioRespostaSegundos` `z.number().nullable()`). Verificar: teste unitário do schema aceita payloads completos, com nulos, mapa `{}` e listas vazias; rejeita shape errado.
- [x] 1.2 `analise.api.ts`: `buscarDashboard()`, `buscarInsightProdutos(ids: string[])` (`ids` vazio → retorna `{}` sem chamar; senão `?ids=` CSV), `buscarInsightEmpresa(id)`. Cada uma faz `GET` via `api-client` e valida com o schema da 1.1. Verificar: `analise.api.test.tsx` com MSW — resposta ok mapeada; 400 no insight-produtos e 422 no insight-empresa viram `ApiError`; `buscarInsightProdutos([])` não dispara request.

## 2. Cabeçalho de dashboard em `/admin`

- [x] 2.1 `PainelDashboard.tsx`: `useQuery(['analise','dashboard'], buscarDashboard, { staleTime: 60_000, retry: 1 })`. `isPending` → faixa de `skeleton`; `isError` → renderiza `null`; sucesso → grid de `card`s (contagem por status; "precisa de ação"; próximos prazos; gasto mês vs. mês anterior + variação; economia 90d; top 5 produtos; top 5 empresas). Helper `prazoRelativo(fechaEm)` ("vence em N dias"/"venceu há N dias" com destaque de atraso). Dinheiro/data via `formatters.ts`. Verificar: `PainelDashboard.test.tsx` (MSW) — cartões com dados; payload zerado → estado vazio; erro 500 → componente não renderiza nada.
- [x] 2.2 Montar `PainelDashboard` no topo de `CotacoesPage.tsx`, acima da lista. Ligar o atalho de "precisa de ação": ler como `CotacoesPage` filtra por status hoje e reusar (setter interno ou query param `?status=`); registrar no design/código o que foi feito. Verificar: `CotacoesPage.test.tsx` — a faixa aparece; acionar o número de "encerradas sem apurar" deixa a lista filtrada por `ENCERRADA`; a lista de cotações segue funcionando quando a análise falha.

## 3. Hover-card de produto

- [x] 3.1 `Sparkline.tsx`: `<svg><polyline/></svg>` a partir de `serie` (0..6 pontos, normaliza min/max); ≤1 ponto → sem linha. Verificar: `Sparkline.test.tsx` — 3 pontos → `<polyline>` com 3 coordenadas; 0/1 ponto → sem `<polyline>`.
- [x] 3.2 `InsightProdutoCard.tsx`: renderiza última compra (Empresa, Representante, preço unitário, data, quantidade), `badge` de `variacaoPct` (Number, 1 casa, ↑ vermelho / ↓ verde / neutro se null), menor preço, média 90d, nº compras, nº fornecedores, `<Sparkline>`, e link "ver cotação" → `/admin/cotacoes/:cotacaoId/resultado`. `null` → "sem histórico de compra" (sem gráfico/atalho); `'erro'` → "insight indisponível". Verificar: `InsightProdutoCard.test.tsx` — os 3 estados; sinal da variação; ausência de valores quando null.
- [x] 3.3 `useInsightProdutos(ids: string[])`: `useQuery(['analise','insight-produtos-lote', [...ids].sort()], () => buscarInsightProdutos(ids), { enabled: ids.length > 0, staleTime: 60_000 })`; ao ter dado, `queryClient.setQueryData(['analise','insight-produto', id], mapa[id] ?? null)` para cada id de `ids` (inclui os ausentes do mapa → `null`). Verificar: `useInsightProdutos.test.tsx` — 1 request para 3 ids; depois `getQueryData(['analise','insight-produto', idX])` devolve o insight; id ausente do mapa → `null` no cache; `ids` `[]` → sem request.
- [x] 3.4 Refatorar `UltimaCompraPopover.tsx`: corpo passa a renderizar `InsightProdutoCard`. Assinatura retrocompatível — aceita `insight` pronto (telas de cotação) OU os campos soltos de última compra que a grade ao vivo já passa (adapta para o card com `serie: []`). Verificar: `GradeAoVivoPage.test.tsx` e os testes de `GradeAoVivoTabela` passam sem alteração.
- [x] 3.5 Ligar o hover em `ItensSection.tsx`: Extrai `ids = cotacao.itens.map(i => i.id)`, chama `useInsightProdutos`, passa para a renderização da linha (via prop `insight={query.data[id] ?? null}`) que agora usa `<UltimaCompraPopover item={{ nome: i.nome }} insight={insight} />`. Verificar: erro tratado (nulo visual); sem request se não houver itens; lote de 3 ids (2 comprados + 1 nunca) → 2 cards com dados, 1 "sem histórico"; erro do insight não impede editar item / adicionar produto.

## 4. Hover-card de empresa

- [x] 4.1 `InsightEmpresaCard.tsx` + `useInsightEmpresa(empresaId, { enabled })`: `useQuery(['analise','insight-empresa', empresaId], () => buscarInsightEmpresa(empresaId), { enabled, staleTime: 300_000, retry: false })`. Card: taxa de resposta (respondeu/convidada), itens vencidos, valor comprado (total e 90d), última compra (data + valor), "mais barata N× / 2º lugar N×", produtos fornecidos, tempo médio via helper `duracaoAprox(seg)` ("~2 h"/"~35 min"/"~1 dia"). Dados zerados/nulos ou erro (incl. 422) → "sem dados de relacionamento". Verificar: `InsightEmpresaCard.test.tsx` — card com dados + formatação do tempo; 422 → "sem dados"; zerado → "sem dados".
- [x] 4.2 Ligar em `ParticipantesSection.tsx`: `enabled` liga no 1º hover/focus do item da Empresa e permanece. Verificar: `ParticipantesSection.test.tsx` — sem hover, nenhuma request; ao passar o mouse, 1 request e o card aparece; 422 não quebra a lista de participantes.

## 5. Fechamento

- [x] 5.1 Popover mecânico: se o esqueleto do `UltimaCompraPopover` estiver acoplado a "última compra", extrair `HoverCard.tsx` (trigger + painel via portal; abre em `mouseenter`/`focus`, fecha em `mouseleave`/`blur`/`Esc`; `role="tooltip"`) e os 3 cards passam a ser conteúdo. Não criar tooltip genérico além disso. Verificar: `HoverCard.test.tsx` (abre/fecha por mouse, foco e Esc) — ou, se não foi extraído, anotar por que não foi preciso.
- [x] 5.2 `npx tsc -b` + `npx vitest run` + `npx oxlint` verdes; nenhum teste pré-existente regrediu (em especial `GradeAoVivoPage`, `ItensSection`, `ParticipantesSection`, `CotacoesPage`). Verificar: os 3 comandos saem com código 0.
- [x] 5.3 `openspec validate painel-insights --strict` sem erros.
- [x] 5.4 Smoke manual: `npm run dev:heroku`, logar com o SeedDemoProducao, conferir o cabeçalho em `/admin`, o hover de produto numa cotação (item e seletor) e o hover de empresa nos participantes — com dados reais. Verificar: os 3 widgets renderizam sem erro no console.
