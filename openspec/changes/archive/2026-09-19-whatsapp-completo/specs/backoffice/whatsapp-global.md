# WhatsApp Global (Super Admin)

Nova página `/backoffice/whatsapp` ou um card no dashboard geral acessível apenas para `SUPER_ADMIN`.
Mostra o status da conexão default (da aplicação), permitindo Gerar QR Code ou Desconectar via `/api/admin/whatsapp/status`, `qr-code` e `disconnect`.
