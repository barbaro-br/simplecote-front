## Tasks

- [x] 1. Alterar `EnvioDeConvite.java` no back-end para instanciar um Logger (ou usar anotação `@Slf4j`) e logar a `NotificacaoException` dentro do bloco catch, usando o formato `logger.error("mensagem", ex)`.
