## 1. Preparação

- [x] 1.1 Ler `src/admin/empresas/` inteiro (Page, Form, api, schema, test) e `src/shared/components/ui/` para decidir o componente de Select (Select do `@base-ui/react` se já houver precedente no repo; senão `<select>` nativo estilizado com os tokens de `src/index.css`). Registrar a decisão no `design.md` (Open Question). Verificar: decisão anotada, nenhum arquivo de código alterado ainda.

## 2. `/admin/representantes`

- [x] 2.1 `src/admin/representantes/representantes.schema.ts`: zod — `representanteSchema` (`id` uuid, `empresaId` uuid, `nome`, `email`, `whatsapp: z.string().nullable()`, `ativo` boolean), `representanteListaSchema`, `criarRepresentanteFormSchema` (nome/email obrigatórios, `email().`, `empresaId` uuid), `editarRepresentanteFormSchema` (nome/email/whatsapp, **sem** `empresaId`). Verificar: `representantes.schema.test.ts` — aceita resposta com `whatsapp` null e `ativo:false`; rejeita shape sem `empresaId`; os form-schemas apontam os campos obrigatórios.
- [x] 2.2 `src/admin/representantes/representantes.api.ts`: hooks TanStack Query `useRepresentantes()` (GET `/api/representantes`, valida com `representanteListaSchema`), `useCriarRepresentante()` (POST, corpo `{empresaId,nome,email,whatsapp}`), `useAtualizarRepresentante()` (PUT `/{id}`, corpo `{nome,email,whatsapp}` — sem `empresaId`), `useInativarRepresentante()` (POST `/{id}/inativar`). Query key `['representantes']`; cada mutation invalida no `onSuccess`. Verificar: `representantes.api.test.tsx` (MSW) — cada hook chama a rota e o corpo certos; PUT não envia `empresaId`; erro 422 vira `ApiError` com `.problem`.
- [x] 2.3 `src/admin/representantes/RepresentanteForm.tsx`: react-hook-form + zod dentro de `Dialog`, prop `modo: 'criar' | 'editar'`. `criar`: campos nome, email, whatsapp (opcional) + Select de Empresa (opções = empresas **ativas** de `useEmpresas()`). `editar`: nome, email, whatsapp; a Empresa atual aparece como texto read-only + linha "Para mover de empresa, fale com o suporte." (sem campo). Botão de enviar desabilitado enquanto `isPending`; área de erro mostra `ApiError.problem.detail`. Verificar: `RepresentanteForm` coberto no `RepresentantesPage.test.tsx` — `criar` mostra o Select com empresas ativas; `editar` não tem Select; validação dispara para nome vazio / email inválido / empresa não escolhida; erro do backend aparece no form.
- [x] 2.4 `src/admin/representantes/RepresentantesPage.tsx`: `useRepresentantes()` + `useEmpresas({ incluirInativos: true })` para o `Map<empresaId,nome>`. Lista (com inativos) mostrando nome, email, whatsapp, nome da Empresa (ou "—"), badge de inativo. Botão "novo representante" abre o `Dialog` (`modo: 'criar'`); ação de editar abre com `modo: 'editar'`; ação de inativar pede confirmação e chama `useInativarRepresentante`. Estados carregando/vazio/erro no mesmo padrão de `EmpresasPage`. **Sem** ação de reativar. Verificar: `RepresentantesPage.test.tsx` (MSW) — lista renderiza com o nome da Empresa; estado vazio; estado de erro não quebra; abrir criar → preencher → confirmar recarrega a lista (invalidation); inativar com confirmação marca como inativo; não há botão de reativar.

## 3. `/admin/usuarios`

- [x] 3.1 `src/admin/usuarios/usuarios.schema.ts`: `papelSchema = z.enum(['ADMIN','OPERADOR'])`; `usuarioSchema` (`id` uuid, `nome`, `email`, `papel`, `ativo`), `usuarioListaSchema`; `criarUsuarioFormSchema` (nome/email obrigatórios, `email()`, `papel`, `senha` `min(8)`), `editarUsuarioFormSchema` (nome/email/papel, sem senha), `redefinirSenhaFormSchema` (`senha` `min(8)` + `confirmar`, `refine` exigindo `senha === confirmar`, erro no campo `confirmar`). Verificar: `usuarios.schema.test.ts` — aceita a resposta do backend; senha < 8 falha; senhas diferentes falham no `refine`.
- [x] 3.2 `src/admin/usuarios/usuarios.api.ts`: `useUsuarios()` (GET `/api/usuarios`), `useCriarUsuario()` (POST `{nome,email,papel,senha}`), `useAtualizarUsuario()` (PUT `/{id}` `{nome,email,papel}`), `useRedefinirSenhaUsuario()` (POST `/{id}/senha` `{senha}`), `useInativarUsuario()` (POST `/{id}/inativar`). Query key `['usuarios']`; mutations invalidam no `onSuccess`. Verificar: `usuarios.api.test.tsx` (MSW) — rotas e corpos certos; `redefinirSenha` manda só `{senha}` (não `confirmar`); 422 vira `ApiError`.
- [x] 3.3 `src/admin/usuarios/UsuarioForm.tsx`: react-hook-form + zod em `Dialog`, `modo: 'criar' | 'editar'`. `criar`: nome, email, papel (select ADMIN/OPERADOR), senha (`type="password"`, min 8). `editar`: nome, email, papel — **sem** senha. Enviar desabilitado enquanto `isPending`; erro do backend na área de erro. Verificar: coberto em `UsuariosPage.test.tsx` — `criar` tem campo de senha, `editar` não; validações disparam (nome, email inválido, senha curta); erro do backend aparece.
- [x] 3.4 `src/admin/usuarios/RedefinirSenhaForm.tsx`: `Dialog` com `senha` + `confirmar` (ambos `type="password"`), `redefinirSenhaFormSchema`. Só envia se passar o `refine` de igualdade e `min(8)`; `mutationFn` = `useRedefinirSenhaUsuario`. Verificar: coberto em `UsuariosPage.test.tsx` — senhas diferentes → erro no `confirmar`, não envia; iguais + 8+ → envia `{senha}` e conclui.
- [x] 3.5 `src/admin/usuarios/UsuariosPage.tsx`: `useUsuarios()`. Lista (com inativos) com nome, email, papel (badge), badge de inativo. "novo usuário" abre `UsuarioForm` (`criar`); ações por linha (menu ou botões): editar (`UsuarioForm` `editar`), trocar senha (`RedefinirSenhaForm`), inativar (confirmação → `useInativarUsuario`). Estados carregando/vazio/erro. **Sem** reativar. Verificar: `UsuariosPage.test.tsx` (MSW) — lista com papéis e status; vazio; erro não quebra; criar recarrega a lista; trocar senha abre o form próprio; inativar com confirmação; sem botão de reativar.

## 4. Fiação (rotas + navegação)

- [x] 4.1 `src/routes.tsx`: adicionar `{ path: 'representantes', element: <RepresentantesPage /> }` e `{ path: 'usuarios', element: <UsuariosPage /> }` dentro do bloco `<AdminLayout>` (atrás do `<AuthGuard>`), no mesmo formato de `produtos`/`empresas` + os `import` correspondentes. Verificar: `git diff src/routes.tsx` mostra só 2 rotas + 2 imports; navegar para `/admin/representantes` e `/admin/usuarios` renderiza as telas.
- [x] 4.2 `src/admin/layout/AdminLayout.tsx`: dois `<NavLink>` novos — "Representantes" (ícone `Users` de `lucide-react`) e "Usuários" (ícone `UserCog`) —, perto de "Produtos"/"Empresas", só markup de nav. Verificar: `AdminLayout` (teste existente) continua passando; os dois links aparecem e apontam para as rotas certas; `git diff` mínimo.

## 5. Fechamento

- [x] 5.1 `npx tsc -b` + `npx vitest run` + `npx oxlint` — código de saída 0. Nenhum teste pré-existente alterado (em especial `EmpresasPage`, `ProdutosPage`, `AdminLayout`). Verificar: os 3 comandos verdes; `git diff` não toca `package.json`, `.github/workflows/**`, `vercel.json`, `openspec/specs/**`.
- [x] 5.2 `openspec validate crud-representantes-e-usuarios --strict` sem erros.
- [x] 5.3 Handoff: listar os arquivos novos (2 dirs, ~10 arquivos), os tracked modificados (`src/routes.tsx`, `src/admin/layout/AdminLayout.tsx`), confirmar que nenhum teste pré-existente mudou, e o código de saída dos 3 comandos.
