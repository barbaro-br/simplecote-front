## Implementação

- [x] 1.1 Em `AdicionarItemModal.tsx`, alterar o atributo `size` do `Dialog` de `lg` para `xl`.
- [x] 1.2 No bloco onde o `p.nome` é renderizado (linha do produto), envolver em uma `<div className="flex flex-col">`.
- [x] 1.3 Adicionar um parágrafo/div logo abaixo do nome com a classe `text-[11px] text-muted-foreground`. A string a ser mostrada deve ser a unidade e quantidade por embalagem (ex: "Caixa com 12").
- [x] 1.4 Garantir que o item tenha a classe zebrada bem visível (`bg-muted/10` nos pares e `bg-background` nos ímpares, por exemplo).
